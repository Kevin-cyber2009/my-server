buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.10.1") // Sử dụng version Gradle mới
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0")
    }
}

